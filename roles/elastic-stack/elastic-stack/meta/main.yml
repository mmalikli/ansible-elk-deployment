galaxy_info:
  author: Murad Malikli
  description: simple role for deploying single node elastic-stack with http access to Kibana
  license: Apache-2.0
  role_name: elastic_stack

  min_ansible_version: "2.1"

  # If this a Container Enabled role, provide the minimum Ansible Container version.
  # min_ansible_container_version:

  platforms:
  - name: Ubuntu
    versions:
    - focal
    - jammy

  galaxy_tags: 
  - elaticsearch
  - logstash
  - kibana

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
